/build*/
cmake-build-*/

CMakeCache.txt
CMakeFiles/
cmake_install.cmake
install_manifest.txt
CTestTestfile.cmake
CMakeUserPresets.json
compile_commands.json
_deps/

Testing/
DartConfiguration.tcl

*.o
*.obj
*.a
*.la
*.lo
*.so
*.so.*
*.dylib
*.dll
*.exe
*.out
*.app

# ---------------------------------------------------------------------
# Precompiled headers
# ---------------------------------------------------------------------
*.gch
*.pch

# ---------------------------------------------------------------------
# Debug, profiling, and crash artifacts
# ---------------------------------------------------------------------
*.dSYM/
*.pdb
*.gcda
*.gcno
*.gcov
callgrind.out.*
perf.data
perf.data.old
core
core.*
vgcore.*

# ---------------------------------------------------------------------
# Generated run output (regenerable synthetic data)
# ---------------------------------------------------------------------
out_bank_data/
*.log

# ---------------------------------------------------------------------
# Commit-message scratch used with `git commit -F`
# ---------------------------------------------------------------------
/commit_message.txt

# ---------------------------------------------------------------------
# Tooling caches. __pycache__ appears from helper scripts/tooling even
# though the shipped binary is pure C++ (see PL_BUILD_PYTHON).
# ---------------------------------------------------------------------
.cache/
__pycache__/

# ---------------------------------------------------------------------
# macOS filesystem litter
# ---------------------------------------------------------------------
.DS_Store
.AppleDouble
.LSOverride
._*
.Spotlight-V100
.Trashes

# ---------------------------------------------------------------------
# Editors and IDEs
# ---------------------------------------------------------------------
.vscode/
.idea/
.vs/
*.swp
*.swo
*~

# ---------------------------------------------------------------------
# Deliberately tracked (no rule above may match these):
#   tests/golden_run.b2sum  - the per-machine end-to-end baseline;
#                             behavior itself belongs in version
#                             control.
# ---------------------------------------------------------------------
CLAUDE.md
AGENTS.md
systemprompt.md
.claude/
.codex/
/graphify-out/
.gitignore
